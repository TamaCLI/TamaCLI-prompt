# TamaCLI-prompt requirements.txt
#
# This file lists the dependencies needed for TamaCLI.
#
# Python dependencies (standard library only)
# No external Python packages required

# Shell dependencies
jq

# ---
# Manual install commands:
#
# macOS (Homebrew):
#   brew install python jq
#
# Linux (apt):
#   sudo apt-get update
#   sudo apt-get install -y python3 jq 